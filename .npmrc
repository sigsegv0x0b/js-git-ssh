; ssh2 declares optional native helpers (cpu-features, and optional native
; crypto bindings) that it falls back to pure-JS without. Without this, a
; plain `npm install` still attempts the native build and warns on failure --
; harmless, but it defeats the "no native modules" property in spirit and
; makes install behavior depend on whether build tools happen to be present.
omit=optional
